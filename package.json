{
  "name": "pwa-app",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:3001/",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "firebase": "^5.5.3",
    "path": "^0.12.7",
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-router": "^3.0.5",
    "react-scripts": "2.0.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "server": "concurrently \"npm run start\" \"node server/server.js\"",
    "build": "react-scripts build",
    "production": "react-scripts build && cross-env NODE_ENV=production PORT=8000 node server/server.js",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
